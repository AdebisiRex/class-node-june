<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
  </head>
  <body class="container">
    <ul class="nav">
      <li class="nav-item">
        <a href="/" class="nav-link">Register</a>
      </li>
      <li class="nav-item">
        <a href="/students" class="nav-link" active>View Students</a>
      </li>
    </ul>
    <h1>Student registered successfully</h1>
    <b>Below is a list of students</b>

    <div class="col-10 mx-auto">
      <table class="table table-warning table-hover">
        <thead>
          <tr>
            <th>S/N</th>
            <th>Last Name</th>
            <th>First Name</th>
            <th>Email</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% for( let i = 0; i < studentList.length; i++ ) { %>
          <tr>
            <td><%= i + 1%></td>
            <td><%= studentList[i].lastName %></td>
            <td><%= studentList[i].firstName %></td>
            <td><%= studentList[i].email %></td>
            <td>
              <form method="get" action="/editPage/<%= studentList[i]._id%>">
                <button class="btn btn-warning text-danger">Edit User</button>
              </form>
              <form method="post" action="/student/<%= i%>">
                <button class="btn btn-danger">Delete</button>
              </form>
            </td>
          </tr>

          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>
